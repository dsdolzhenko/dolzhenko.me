# -*- mode: sh -*-
#!/usr/bin/env bash

set -o pipefail

read ref
branch=$(echo $ref | cut -d ' ' -f 3)

if [ "$branch" = "refs/heads/master" ]
then
    cd /home/dolzhenko/code/dolzhenko.me
    git pull
    ./scripts/build.sh
fi
